This is the install directory of the project.
Here all executables compiled with `make install` are stored.

*Note:* all files (except this readme) in this directory are included in the `../.gitignore` file.
